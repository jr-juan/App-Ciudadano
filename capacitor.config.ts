import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "com.ecoruta.ciudadano",
  appName: "EcoRuta Ciudadano",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
